WinMain() 제일 먼저호출되는 함수

WNDCLASSEX wc
>> 단지 구조체를 정의한 것

CreateWindow() 다음 호출되는 함수
>> Os에게 만들도록 하는 것?
>> WinAPI를 이용하여 윈도우 창을 생성(초기화?)한다

WinAPI: 윈도우 OS(운영체제)에서 제공하는 Data Type과 Function을 포함하는 시스템 라이브러리

direct3D초기화: 3d디바이스(GPU)를 초기화한다

정점 버퍼 초기화
>> vertex버퍼에 값을 초기화하고 3d디바이스 장치(GPU)에 버텍스버퍼를 생성/복제한다
>>

메시지 루프: 윈도우가 작동하는 중에 메시지를 받기위한 루프
>> 외부요소에 의한 특정 입력? 이벤트?가 있을 때까지 무한루프


Render()
>>랜더링 : 백버퍼를 초기화하고 다음 프레임을 랜더링하고, 전면버퍼와 교환한다.
	=>앞서 생성되었던 것과 뒤에 생성된 것의 자리를 바꾼다?


LP로 받아오는 이유는 프로세스와 GPU가 하는 일이 다르기 때문에 주소값을 가져옴?
포인터 쓰는 이유? >> A가 B가 뭐하는지 받아오고 싶지만 B에게 접근할 수 없을떄
				>> 포인터를 통한 주소값으로 정보를 확인할 수 있음

Lock을 걸어주는 이유
>> 해당 공간을 단독으로 사용하지 않고 여러 곳에서 해당 공간을 같이 사용하여 문제가 발생
>> Lock을 걸어주면 먼저 도착한? 사용하게 된 A사용자를 제외하고 해당 공간을 사용할 수 없다
>> A사용자가 작업을 끝낸 후에는 뒤이어 사용하려는 사용자들에게도 할당?해야하기 때문에
>> unlock을 통해 잠금상태를 해제한다

벡터의 개념?
>> 외적: 
>> 내적: 